# backend/.env.example

# ===================================================
# CONFIGURAÇÕES GERAIS
# ===================================================

# Porta na qual o servidor Express será executado.
# O frontend React usará este valor para configurar o proxy no vite.config.js.
PORT=5000

# Chave secreta usada pelo JSON Web Token (JWT) para assinar e verificar
# tokens. Essencial para autenticação e segurança de rotas protegidas.
# Use uma string longa, aleatória e segura em produção.
# JWT_SECRET=sua_chave_secreta_aqui

# ===================================================
# CONFIGURAÇÕES DO MONGODB
# ===================================================

# URI de conexão completa para o banco de dados MongoDB (Atlas/nuvem).
# Substitua <USUARIO_ATLAS> e <SENHA_ATLAS> pelas suas credenciais reais.
# O nome do banco de dados (e.g., 'trabalho_pratico_db') está incluído na URI.

# URI de conexão do MongoDB.
MONGODB_URI=mongodb+srv://<USUARIO_ATLAS>:<SENHA_ATLAS>@mern-cluster.abcd1.mongodb.net/trabalho_pratico_db?retryWrites=true&w=majority